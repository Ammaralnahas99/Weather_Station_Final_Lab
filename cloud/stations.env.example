# Copy to stations.env and fill in the real value. Never commit stations.env.

# Must match KAFKA_ADVERTISED_HOST set in central.env on the Central Station VM
# (private IP if same VPC/region, public IP otherwise).
KAFKA_HOST=10.0.0.10
